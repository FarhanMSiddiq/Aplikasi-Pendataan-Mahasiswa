<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.farhanms.responsi">

    <application
        android:allowBackup="true"
        android:icon="@drawable/reading"
        android:label="@string/app_name"
        android:roundIcon="@drawable/reading"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ModulRead.ReadActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".ModulRead.DetailActivity"
            android:label="Detail Mahasiswa"
            android:parentActivityName=".ModulRead.ReadActivity" />
        <activity
            android:name=".ModulCreate.CreateActivity"
            android:label="Buat Data Baru"
            android:parentActivityName=".ModulRead.ReadActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>